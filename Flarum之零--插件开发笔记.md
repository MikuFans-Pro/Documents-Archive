# Flarum 之零 - 一个 Flarum 插件的开发笔记：从"一行代码"到放弃

> 导读：本文是「Flarum部署连环坑」系列的一部分。完整的问题梳理与解决方案总结，请参阅：[Flarum之八--20坑复盘与国内部署最小踩坑路径（最终章？）](https://lab.metazone.cc/d/13)

写了个 Flarum 扩展，功能很简单：编辑已过审的帖子时自动退回审核队列，防止改内容绕过审核。

核心逻辑就一行：`$post->is_approved = null;`

监听 `Flarum\Post\Event\Saving`，判断帖子是否已过审、内容是否变更、操作者是否管理员，满足条件就设 null。完事。

## 然后发现一手的坑

上线之后问题就来了。

**第一个坑：标题不过审。** Flarum 审核机制只管正文，标题改完直接展示。你正文设 null 退回审核，标题早挂出去了。想搞事的往标题放个短链接，审核形同虚设。

**第二个坑：帖子变空壳。** 正文退回审核后，帖子还在讨论列表里，谁都能看到标题。点进去看到的是"等待审核"，内容没了。用户观感就是帖子凭空消失了，非常反直觉。

## 试图做个"完美方案"

想了个方案：旧内容保持可见，新内容进修订表待审，审核通过再替换。需要的东西不少：

- 新建 `post_revisions` 表存修订版
- 前后端分开渲染旧/新内容
- 管理员审核 UI
- 通知系统

搞了几轮，发现 Flarum 的渲染管线耦合太深。s9e text-formatter、PostSerializer、前端 DiscussionPage 等都要侵入式修改。修一处炸三处，维护成本完全不是一个小插件能承担的。

## 结论

提了 README 标签暂停开发，建议直接关掉编辑权限——用户想改就删了重发，少一个功能少一堆漏洞。

有时候最简单的需求背后是个架构问题，一行代码能触发，一百行也修不好。
